Project context document view: backend fixes, context stats, scrollable GKG dialog

Backend
- projects.ts: decodeURIComponent(documentId) in GET document and GET document
  versions so project_context documents (URL-encoded IDs) resolve correctly.
- jiraLinkage.ts: decodeURIComponent(documentId) in GET document Jira linkage.
- context-injection.ts: getProjectContext resilient to missing project_stakeholders
  table and documents.type column; check information_schema before querying
  project_stakeholders to avoid DB-GUARD log when table absent.
- projects.ts: synthetic project context document now includes metadata.context_stats
  (documents in project, used as context, stakeholders, context tokens).
- projects.ts: GET document versions for project_context returns synthetic version
  with full markdown content and word_count so document view shows correct
  Words and content (no longer overwritten by empty version).

Documentation
- GKG_CONTEXT_INJECTION_AND_VIEW_SUMMARY.md: new §1.4 Backend document ID decoding,
  §1.5 Documentation; quick reference and related files updated.
- GKG_CONTEXT_STRATEGY.md: §7 Retrieval notes documentId decodeURIComponent in
  projects and Jira linkage routes.

Frontend (document view)
- Context Statistics: show from metadata.context_stats or derive from
  generation_metadata.gkg_context_snapshot when present (project context / GKG docs).
- Injected context (GKG) dialog: scrollable body and dedicated scrollable area for
  "Context text sent to the LLM" (h-[50vh] ScrollArea, max-h-[90vh] dialog).

Branch also includes other GKG/process-flow/template changes.
