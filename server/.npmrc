# Skip Puppeteer browser download to avoid network issues
# Puppeteer is used for PDF generation but requires manual browser setup
PUPPETEER_SKIP_DOWNLOAD=true

# Use legacy peer deps to handle dependency conflicts
legacy-peer-deps=true
