import { Extractor } from './extractor';

class ScopeItemsExtractor implements Extractor {
  async run(): Promise<void> {
    // TODO: implement extraction for Scope Items
    console.log('ScopeItemsExtractor: run() - TODO implement');
  }
}

export default new ScopeItemsExtractor();
