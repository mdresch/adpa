import { Extractor } from './extractor';

class ActivitiesExtractor implements Extractor {
  async run(): Promise<void> {
    // TODO: implement extraction for Activities
    console.log('ActivitiesExtractor: run() - TODO implement');
  }
}

export default new ActivitiesExtractor();
