[variables]
NIXPACKS_NODEJS_VERSION = "20"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --prefix server"]

[phases.build]
cmds = ["npm run build --prefix server || true"]

[start]
cmd = "npm start --prefix server"


